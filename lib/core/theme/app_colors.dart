import 'package:flutter/material.dart';

class AppColors {
  // Fond & surfaces
  static const Color background  = Color(0xFFFAF9F6); // off-white crème
  static const Color surface     = Color(0xFFFFFFFF);
  static const Color secondary   = Color(0xFFF0EFF0); // gris très clair

  // Texte
  static const Color foreground  = Color(0xFF1A1F36); // bleu-noir profond
  static const Color muted       = Color(0xFF9CA3AF); // gris neutre

  // Primary — bleu profond
  static const Color primary     = Color(0xFF2563EB);
  static const Color primarySoft = Color(0xFFEFF6FF);

  // Accent — violet doux
  static const Color accent      = Color(0xFF7C3AED);
  static const Color accentSoft  = Color(0xFFF5F3FF);

  // Success — vert sauge
  static const Color success     = Color(0xFF16A34A);
  static const Color successSoft = Color(0xFFF0FDF4);

  // Warning — ambre doux
  static const Color warning     = Color(0xFFD97706);
  static const Color warningSoft = Color(0xFFFFFBEB);
}
